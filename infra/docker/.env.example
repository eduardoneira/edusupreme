# Local development defaults for Docker Compose.
# Copy to infra/docker/.env if you want to override values locally.

POSTGRES_DB=edusupreme
POSTGRES_USER=edusupreme
POSTGRES_PASSWORD=edusupreme_dev_password
POSTGRES_PORT=5432
